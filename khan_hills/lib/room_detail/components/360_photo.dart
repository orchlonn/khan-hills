import 'package:flutter/material.dart';

class PanomaraPhoto extends StatelessWidget {
  const PanomaraPhoto({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
